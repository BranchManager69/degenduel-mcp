// src/env/config.ts

/**
 * General application configuration for the MCP server
 */


// Flags
const HTTP_MODE_ENABLED = true;

// Server
const PORT = 3333;


// Export the config
export { HTTP_MODE_ENABLED, PORT };